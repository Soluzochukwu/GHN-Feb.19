<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amirkalmoni.ghnauthenticationfinal">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".loginactivicty" />
        <activity android:name=".ProfileActivicty" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".Account_Settings" />
        <activity android:name=".Groups_Settings" />
        <activity android:name=".Notification_Settings" />
        <activity android:name=".DS_Settings" />
        <activity android:name=".Help_Settings" />
        <activity android:name=".EditProfile_Account" />
        <activity android:name=".Delete_Account"></activity>
    </application>

</manifest>